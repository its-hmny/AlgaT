package Classes;

public interface Test_Interface extends Basic_Interface {
    //Used to check the user input, in the Test part of the application, as you can guess
    //returns true (if the user ha responded correctly) else false
    boolean checkAnswer();
}
